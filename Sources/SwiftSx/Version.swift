/// Version metadata for SwiftSx.
public enum SxVersion {
    /// The SwiftSx release version, reported by `sx --version`.
    ///
    /// SwiftSx is an in-progress port of `byteowlz/sx` (which is at 2.4.0);
    /// this is the port's own version and advances independently.
    public static let current = "0.1.0"
}
